import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  bottomTabContainer: {
    position: 'absolute',
    bottom: 10,
    margin: 10,
    marginHorizontal: 25,
    borderRadius: 20,
    padding: 10,
    backgroundColor: '#EDEDED',
    flexDirection: 'row',
    alignItems: 'center',
  },
  aiButton: {
    position: 'absolute',
    left: '43%',
    top: -25,
    backgroundColor: '#fff',
    padding: 8,
    borderRadius: 20,
  },
});